"use client";

import {
  motion as framerMotion,
  AnimatePresence,
  useMotionValue,
  useTransform,
  useAnimation,
  // Add other named imports you need
} from "framer-motion";

export const CustomMotion = framerMotion;
export { AnimatePresence, useMotionValue, useTransform, useAnimation };
